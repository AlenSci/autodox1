import React from 'react'
import {Route} from "react-router-dom";

function SendEmail() {
  // let { username }: any = useParams()

    return (
        <Route path="/Send%20email">
            Send emails are here
        </Route>
    );
}

export default SendEmail
